<h1><%= t('views.show_title') %></h1>

<p>
  <strong><%= t('common.title') %></strong>
  <%= @task.title %>
</p>
<p>
  <strong><%= t('common.content') %></strong>
  <%= @task.content %>
</p>
<p>
  <strong><%= t('common.created_at') %></strong>
  <%= @task.created_at %>
</p>

<%= link_to t('common.edit'), edit_task_path(@task), id: "edit-task" %> |
<%= link_to t('common.back'), tasks_path, id: "back" %>

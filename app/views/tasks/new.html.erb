<h1><%= t('views.new_title') %></h1>

<%= render 'form', task: @task, button_id: "create-task" %>
<div class="actions">
    <%= form.submit t('common.create'), id: button_id %>
  </div>
<%= link_to t('common.back') , tasks_path, id: "back" %>

<div class="container-fluid">
<h1><%= t('.title') %></h1>

<%= render 'form', task: @task, button_id: "create-task", class: 'btn btn-primary' %>

<%= link_to t('common.back') , tasks_path, id: "back", class: 'btn btn-secondary' %>
</div>

